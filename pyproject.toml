[project]
name = "html-fetcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.15.0",
    "fastapi>=0.136.0",
    "playwright>=1.58.0",
    "pydantic>=2.13.2",
    "uvicorn>=0.44.0",
]
